CREATE TABLE teams (
                       id BIGINT AUTO_INCREMENT PRIMARY KEY,
                       nombre VARCHAR(255),
                       liga VARCHAR(255),
                       pais VARCHAR(255)
);